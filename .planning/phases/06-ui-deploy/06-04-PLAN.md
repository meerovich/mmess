---
phase: 06-ui-deploy
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified: []
autonomous: false

requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "The interface is fully usable on a mobile browser without horizontal scrolling"
    - "Theme toggle switches light/dark/system and persists across page reload"
    - "Conversation search filters the list by name or participant username"
    - "No flash of wrong theme on page load"
    - "All prior functionality (messaging, files, groups, presence) continues to work"
  artifacts: []
  key_links: []
---

<objective>
Final human verification checkpoint for Phase 6. The executor runs a structured UAT against the three UI-01, UI-02, UI-03 requirements, then marks the phase complete.

Purpose: Phase 6 is the final v1 milestone. A human must confirm the interface works on mobile, dark mode applies correctly, and search behaves as designed before the phase is declared done.
Output: SUMMARY.md documenting UAT results. Phase 6 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-ui-deploy/06-CONTEXT.md
@.planning/phases/06-ui-deploy/06-01-SUMMARY.md
@.planning/phases/06-ui-deploy/06-02-SUMMARY.md
@.planning/phases/06-ui-deploy/06-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT: Phase 6 final sign-off (UI-01, UI-02, UI-03)</name>
  <action>Human verification required — no automated implementation. See how-to-verify for the 23-item test checklist.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; no automated command applies</automated>
  </verify>
  <done>Human types "approved" confirming all 23 UAT checks pass.</done>
  <what-built>
    Plan 06-01: Dark theme tokens, CSS debt cleanup, index.html mobile meta + PWA manifest, safe-area insets on ChatLayout.
    Plan 06-02: ThemeToggle component (3-state), conversation search with Ctrl+K, sidebar footer with user info.
    Plan 06-03: Docker healthchecks for api+caddy, .env.production.example, backup/restore scripts, README deploy guide.
  </what-built>
  <how-to-verify>
    Run the following test cases and confirm each passes. Report any failures.

    **UI-02: Theme toggle**

    1. Open the app. Confirm the sidebar footer shows your username + the 3-button toggle (sun/moon/monitor).
    2. Click the Moon button. Confirm the entire UI switches to dark background (#1a1a1a surfaces). The moon button should show accent-blue background.
    3. Hard-refresh the page (Ctrl+Shift+R / Cmd+Shift+R). Confirm dark mode is still active — NO flash of white before dark.
    4. Click the Sun button. Confirm UI switches to light mode. The sun button gets accent background.
    5. Click the Monitor button. Confirm UI matches your OS preference. Change OS dark/light mode — confirm UI updates live without page reload.
    6. Check unread badge and action buttons in dark mode — text should be dark (#0a0a0a), not white.

    **UI-03: Conversation search**

    7. Type a few characters matching a conversation name. Confirm the list filters in real time.
    8. Type a participant's username from a DM conversation. Confirm the DM appears in results.
    9. Type something with no match. Confirm "No conversations match" message appears.
    10. Click the x clear button. Confirm the full list reappears.
    11. Press Ctrl+K (or Cmd+K on Mac). Confirm the search input is focused.

    **UI-01: Mobile responsiveness**

    12. Open Chrome DevTools, toggle device toolbar, select iPhone 14 or similar (390px wide).
    13. Confirm the sidebar fills the full screen on mobile (no horizontal scroll).
    14. Confirm the conversation list is scrollable without clipping.
    15. Tap on a conversation — confirm the chat pane appears full-screen (sidebar hides).
    16. Check that the search input does NOT trigger zoom on focus (font-size must be >= 16px).
    17. Confirm no UI elements are cut off by the notch/safe-area on iPhone viewport.

    **Regression check**

    18. Send a text message in a conversation. Confirm it appears in real time in both light and dark mode.
    19. Upload an image. Confirm the inline preview and lightbox work in dark mode.
    20. Confirm group settings modal is usable in dark mode (no invisible text).

    **Deploy artifacts**

    21. `cat .env.production.example` — confirm all vars are present with generation hints.
    22. `cat scripts/backup.sh` — confirm pg_dump command uses `docker compose exec`.
    23. `grep "healthcheck" docker-compose.yml` — confirm api and caddy blocks are present.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 23 checks pass.
    Or describe any failures (e.g. "dark mode flash on reload", "search not filtering") and I will create gap-closure tasks.
  </resume-signal>
</task>

</tasks>

<verification>
Human UAT as listed above. No automated verification for this plan — it is purely a human sign-off gate.
</verification>

<success_criteria>
Human reviewer confirms:
- Theme toggle switches correctly; dark mode persists across reload without flash
- Conversation search filters by name + participant username; empty state shows correct message
- Mobile layout works on 390px viewport without horizontal scroll or zoom on input focus
- All prior features (messaging, reactions, files, presence, groups) work in both themes
- Deploy artifacts (.env.production.example, backup.sh, docker healthchecks) are present and correct
</success_criteria>

<output>
After human approval, create `.planning/phases/06-ui-deploy/06-04-SUMMARY.md` documenting UAT results.

Then update STATE.md: Phase 6 complete, all v1 requirements done.
</output>
