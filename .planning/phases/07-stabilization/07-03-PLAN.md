---
phase: 07-stabilization
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - .planning/phases/07-stabilization/07-VERIFICATION.md
  - .planning/HOTFIXES.md
  - scripts/deploy.sh
autonomous: false
requirements: [STAB-01, STAB-02, STAB-03, STAB-04]

must_haves:
  truths:
    - "Production deploy completes with docker compose ps showing all 3 services healthy"
    - "/api/health returns the new bumped version"
    - "E2E WS regression script passes on VPS (exit 0)"
    - "Two-browser manual test confirms read receipts update from gray to blue within 1 second"
    - "Manual regression sweep of all base features passes"
    - "Every bug found during sweep is fixed and documented"
  artifacts:
    - path: ".planning/phases/07-stabilization/07-VERIFICATION.md"
      provides: "Phase verification with test results and regression sweep findings"
      contains: "PASS"
    - path: ".planning/HOTFIXES.md"
      provides: "Updated with any bugs found during sweep"
  key_links:
    - from: "scripts/deploy.sh"
      to: "https://chatboris.mooo.com"
      via: "docker compose up -d"
      pattern: "deploy"
---

<objective>
Deploy the read receipt fixes to production, run the full regression suite (automated E2E + manual UI sweep), fix any found bugs in-phase, and document results. This is the phase gate.

Purpose: STAB-01/02 (verify read receipts work in production), STAB-03 (regression sweep passes), STAB-04 (all findings documented).
Output: Production deploy with verified read receipts, 07-VERIFICATION.md with full results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-stabilization/07-CONTEXT.md
@.planning/phases/07-stabilization/07-01-SUMMARY.md
@.planning/phases/07-stabilization/07-02-SUMMARY.md
@.planning/HOTFIXES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to production and run automated E2E</name>
  <files>scripts/deploy.sh, .planning/phases/07-stabilization/07-VERIFICATION.md</files>
  <read_first>
    scripts/deploy.sh,
    .planning/HOTFIXES.md,
    .planning/phases/07-stabilization/07-01-SUMMARY.md,
    .planning/phases/07-stabilization/07-02-SUMMARY.md
  </read_first>
  <action>
**1. Build and deploy using the existing deploy script:**

Run the deploy flow per the established pattern in `.planning/HOTFIXES.md`:
- Ensure all changes are committed to `main`
- Run `bash scripts/deploy.sh` which bumps version, builds client, rebuilds api image, and restarts via docker compose on VPS

**2. Verify deployment health:**

After deploy completes:
- `docker compose ps` — all 3 services (postgres, api, caddy) must show `(healthy)` or `running`
- `curl -s https://chatboris.mooo.com/api/health` — must return `{"status":"ok","version":"<new-version>"}`

**3. Run the automated E2E regression script on VPS:**

```bash
docker exec mmess-api-1 node /app/.vps-test-ws.mjs
```

Script must exit 0 with `=== ALL TESTS PASSED ===`. If it exits 1, read the `[FAIL]` output, fix the bug (document in HOTFIXES.md per STAB-04), redeploy, and re-run until exit 0.

**4. Document results in 07-VERIFICATION.md:**

Create `.planning/phases/07-stabilization/07-VERIFICATION.md` with:
- Deploy version number
- `docker compose ps` output
- `/api/health` response
- E2E test script output (full log)
- PASS/FAIL status for each test section
  </action>
  <verify>
    <automated>MISSING — requires VPS access; verification is the deploy+E2E output captured in 07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md exists and contains the new version number
    - 07-VERIFICATION.md contains "ALL TESTS PASSED" from E2E output
    - 07-VERIFICATION.md contains docker compose ps output showing healthy services
    - /api/health returns version higher than 1.0.3
  </acceptance_criteria>
  <done>
    Production deploy complete, all 3 services healthy, E2E regression script passes.
    07-VERIFICATION.md documents deploy results and E2E output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual regression sweep + read receipt visual verification</name>
  <files>.planning/phases/07-stabilization/07-VERIFICATION.md, .planning/HOTFIXES.md</files>
  <action>
Present the manual verification checklist to the user. After user completes the sweep, update 07-VERIFICATION.md with results. If defects are found, fix them, document in HOTFIXES.md (STAB-04), redeploy, and re-verify.
  </action>
  <verify>
    <automated>MISSING — manual human verification checkpoint</automated>
  </verify>
  <done>User confirms all regression checks pass and read receipts display correctly.</done>
  <what-built>
    Read receipt pipeline fix (3-state: sent/delivered/read) deployed to production.
    Automated E2E regression script already passed in Task 1.
  </what-built>
  <how-to-verify>
**Read Receipt Verification (STAB-01, STAB-02) — use two browsers (e.g., Chrome + Firefox or desktop + mobile):**

1. Open https://chatboris.mooo.com in Browser A (log in as admin) and Browser B (log in as tester)
2. In Browser A, open the DM with tester
3. Send a message from Browser A — verify:
   - Immediately shows single gray check (gray = sent)
   - Within 1 second, transitions to double gray check (gray = delivered) because tester's socket is open
4. In Browser B, open the DM with admin (the conversation where the message was sent)
5. Verify in Browser A:
   - Within 1 second of Browser B opening the conversation, the check-mark transitions to double BLUE check (accent color = read)
6. Hover over the check-mark — verify tooltip shows "Read by: tester"

**Manual Regression Sweep (STAB-03) — check each feature on the live site:**

- [ ] Auth: Login works on both browsers
- [ ] DM: Can send and receive text messages in both directions
- [ ] Group: Create group, send message, all members receive
- [ ] Text: Messages display correctly with timestamps
- [ ] Reactions: Add emoji reaction, visible to all participants immediately
- [ ] Files: Upload image, thumbnail appears inline, click opens lightbox
- [ ] Typing: "X is typing..." appears when other user types, disappears after stop
- [ ] Presence: Online dot shows for connected users, goes offline after disconnect
- [ ] Unread counter: Sidebar badge increments on new messages in non-active conversation
- [ ] Notifications: Browser notification fires for incoming messages
- [ ] Themes: Theme toggle switches between light/dark
- [ ] Mobile layout: On narrow viewport — sidebar/chat pane toggle, back button, input stays in view

**If any defect is found:** Describe it — Claude will fix it in-phase, document in HOTFIXES.md (STAB-04), redeploy, and re-verify.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Production deploy succeeds with new version
- E2E regression script exits 0 on VPS
- Read receipts visually show 3-state transitions in two-browser test
- Manual regression sweep finds zero user-visible defects (or all found defects are fixed)
- 07-VERIFICATION.md documents everything
</verification>

<success_criteria>
- All 5 phase success criteria from ROADMAP met:
  1. Sender sees sent -> delivered -> read check-marks matching actual recipient state
  2. Read-mark updates sender within 1 second without reload
  3. Regression sweep completes with zero user-visible defects
  4. All fixes documented in HOTFIXES.md or VERIFICATION.md
  5. Production deploy with healthy services, correct version, E2E PASS
</success_criteria>

<output>
After completion, create `.planning/phases/07-stabilization/07-03-SUMMARY.md`
</output>
