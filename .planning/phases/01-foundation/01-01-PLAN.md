---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - .eslintrc.json
  - .prettierrc
  - tsconfig.base.json
  - server/package.json
  - server/tsconfig.json
  - client/package.json
  - client/tsconfig.json
  - client/vite.config.ts
  - client/index.html
  - client/src/main.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Running `npm install` in both server/ and client/ succeeds without errors"
    - "`npx tsc --noEmit` passes in both server/ and client/ directories"
    - "ESLint and Prettier are configured and can run across the codebase"
    - "The monorepo directory layout matches the agreed structure (server/, client/, root config files)"
  artifacts:
    - path: "server/package.json"
      provides: "Backend dependency manifest with Fastify 5, Drizzle ORM, ws, TypeScript"
    - path: "client/package.json"
      provides: "Frontend dependency manifest with React 19, Vite 6, React Router 6"
    - path: "server/tsconfig.json"
      provides: "TypeScript config for Node.js 22 (ESM, strict mode)"
    - path: "client/tsconfig.json"
      provides: "TypeScript config for React/Vite (DOM lib, JSX)"
    - path: ".eslintrc.json"
      provides: "ESLint config for TypeScript"
    - path: ".prettierrc"
      provides: "Prettier formatting rules"
  key_links:
    - from: "server/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: "extends.*tsconfig.base"
    - from: "client/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: "extends.*tsconfig.base"
---

<objective>
Bootstrap the mmess monorepo: root tooling config, server/ (Fastify + Drizzle + ws), and client/ (React 19 + Vite 6 + React Router) directory with all dependencies installed and TypeScript compiling clean.

Purpose: Every subsequent phase builds on this scaffold. Getting TypeScript, ESLint, and package structure right now prevents churn in later phases.
Output: A working monorepo with clean `tsc --noEmit` in both workspaces, linting config in place, no runtime code yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root scaffold — .gitignore, Prettier, ESLint, base tsconfig</name>
  <files>
    package.json
    .gitignore
    .prettierrc
    .eslintrc.json
    tsconfig.base.json
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01, D-02, D-03)
    - .planning/research/STACK.md (TypeScript 5.x, Node.js 22)
  </read_first>
  <action>
Create the monorepo root. Use npm workspaces (package manager: npm per Claude's discretion).

**package.json** (root — workspaces, scripts only, no app deps):
```json
{
  "name": "mmess",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
```

**.gitignore**:
```
node_modules/
dist/
build/
.env
.env.local
*.local
*.tsbuildinfo
```

**.prettierrc**:
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

**.eslintrc.json**:
```json
{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended"
  ],
  "env": {
    "node": true,
    "es2022": true
  },
  "rules": {
    "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }],
    "@typescript-eslint/explicit-function-return-type": "off",
    "@typescript-eslint/no-explicit-any": "warn"
  }
}
```

**tsconfig.base.json** (shared strict settings):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

After creating files, run `npm install` in the root to install devDependencies.
  </action>
  <verify>
    <automated>cd E:/dev/mmess && ls package.json .gitignore .prettierrc .eslintrc.json tsconfig.base.json && node -e "require('./package.json').workspaces && console.log('workspaces OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `"workspaces": ["server", "client"]`
    - `.eslintrc.json` exists and is valid JSON
    - `.prettierrc` exists and is valid JSON
    - `tsconfig.base.json` has `"strict": true` and `"target": "ES2022"`
    - `node_modules/` created after `npm install`
  </acceptance_criteria>
  <done>Root config files created and npm install succeeds at root level.</done>
</task>

<task type="auto">
  <name>Task 2: Server workspace — Fastify skeleton, Drizzle, ws, TypeScript config</name>
  <files>
    server/package.json
    server/tsconfig.json
    server/src/index.ts
  </files>
  <read_first>
    - .planning/research/STACK.md (exact package names and versions: fastify 5.8.x, drizzle-orm 0.45.x, ws 8.20.x, postgres npm driver)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01, D-02, D-10)
    - tsconfig.base.json (just created — server tsconfig extends it)
  </read_first>
  <action>
Create `server/` workspace. This is a skeleton only — no routes, no DB queries, just verified imports and server start.

**server/package.json**:
```json
{
  "name": "@mmess/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^9.0.0",
    "@fastify/multipart": "^9.0.0",
    "@fastify/static": "^8.0.0",
    "@fastify/websocket": "^11.0.0",
    "drizzle-orm": "^0.45.0",
    "fastify": "^5.8.0",
    "postgres": "^3.4.0",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.0.0",
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
```

**server/tsconfig.json**:
```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "lib": ["ES2022"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**server/src/index.ts** (minimal skeleton — just verifies the app boots):
```typescript
import Fastify from 'fastify';

const app = Fastify({
  logger: {
    level: process.env.LOG_LEVEL ?? 'info',
  },
});

app.get('/health', async (_request, _reply) => {
  return { status: 'ok', timestamp: new Date().toISOString() };
});

const start = async () => {
  try {
    const port = Number(process.env.PORT ?? 3000);
    const host = process.env.HOST ?? '0.0.0.0';
    await app.listen({ port, host });
  } catch (err) {
    app.log.error(err);
    process.exit(1);
  }
};

start();
```

After creating files, run `npm install` inside `server/` (or `npm install -w server` from root) to install all dependencies.

Then run `npx tsc --noEmit` inside `server/` to confirm zero TypeScript errors.
  </action>
  <verify>
    <automated>cd E:/dev/mmess/server && npm install && npx tsc --noEmit && echo "TSC CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `server/package.json` lists `fastify`, `drizzle-orm`, `ws`, `postgres` as dependencies
    - `server/tsconfig.json` extends `../tsconfig.base.json`
    - `npx tsc --noEmit` in `server/` exits 0 with no errors
    - `server/src/index.ts` imports Fastify and defines a `/health` GET route
    - `server/node_modules/fastify` directory exists after install
  </acceptance_criteria>
  <done>Server workspace installs and compiles cleanly. `server/src/index.ts` is a valid Fastify app skeleton.</done>
</task>

<task type="auto">
  <name>Task 3: Client workspace — React 19 + Vite 6 + React Router 6, TypeScript config</name>
  <files>
    client/package.json
    client/tsconfig.json
    client/vite.config.ts
    client/index.html
    client/src/main.tsx
    client/src/App.tsx
  </files>
  <read_first>
    - .planning/research/STACK.md (React 19.x, Vite 6.x, React Router 6.x)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01, D-02, D-12)
    - tsconfig.base.json (client tsconfig extends it with DOM overrides)
  </read_first>
  <action>
Create `client/` workspace. Minimal React SPA — no actual UI, just verifies it builds.

**client/package.json**:
```json
{
  "name": "@mmess/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^6.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
```

**client/tsconfig.json**:
```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "noEmit": true,
    "outDir": "dist"
  },
  "include": ["src/**/*", "vite.config.ts"],
  "exclude": ["node_modules", "dist"]
}
```

**client/vite.config.ts**:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',
  },
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:3000',
      '/ws': {
        target: 'ws://localhost:3000',
        ws: true,
      },
    },
  },
});
```

**client/index.html**:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>mmess</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**client/src/main.tsx**:
```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App.tsx';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>
);
```

**client/src/App.tsx**:
```typescript
export default function App() {
  return (
    <div>
      <h1>mmess</h1>
      <p>Coming soon.</p>
    </div>
  );
}
```

After creating files, run `npm install` inside `client/` then `npx tsc --noEmit` and `npm run build` to confirm clean compile and successful Vite build.
  </action>
  <verify>
    <automated>cd E:/dev/mmess/client && npm install && npx tsc --noEmit && npm run build && echo "CLIENT BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `client/package.json` lists `react`, `react-dom`, `react-router-dom`, `vite` as dependencies
    - `client/tsconfig.json` has `"jsx": "react-jsx"` and extends `../tsconfig.base.json`
    - `client/vite.config.ts` defines proxy for `/api` and `/ws`
    - `npx tsc --noEmit` in `client/` exits 0
    - `npm run build` in `client/` succeeds, creating `client/dist/` directory
    - `client/dist/index.html` exists after build
  </acceptance_criteria>
  <done>Client workspace installs, TypeScript-checks, and builds cleanly via Vite. A static `dist/` is produced.</done>
</task>

</tasks>

<verification>
From the project root (`E:/dev/mmess`):
1. `ls server/package.json client/package.json tsconfig.base.json .eslintrc.json .prettierrc` — all exist
2. `cd server && npx tsc --noEmit` — exits 0
3. `cd ../client && npx tsc --noEmit` — exits 0
4. `cd ../client && npm run build` — exits 0, `dist/index.html` created
5. `node -e "const p = require('./package.json'); console.log(p.workspaces)"` — prints `["server","client"]`
</verification>

<success_criteria>
- Monorepo with `server/` and `client/` workspaces (per D-01)
- TypeScript configured for both workspaces, both compile clean (per D-02)
- ESLint + Prettier configured at root (per D-03)
- `client/dist/` produced by `vite build` — Caddy can serve these static files in Plan 03
- No runtime code yet; this is purely scaffold
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
